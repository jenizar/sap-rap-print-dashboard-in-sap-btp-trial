managed implementation in class zbp_i_pdhdr unique;
//strict ( 2 );

define behavior for YI_PDHDR alias Header
persistent table ytbpdhdr
lock master
authorization master ( instance )
//etag master <field_name>
{
  create;
  update;
  delete;
  field ( mandatory : create, readonly : update ) CompanyCode;
  field ( mandatory ) CompanyName, AddressLocation, TaxId;
  field ( readonly ) LocalCreatedBy, LocalCreatedAt, LocalLastChangedAt, LocalLastChangedBy, LastChangedAt;
  association _Item { create; }

   action print_dashboard;

  //mapping all field from YTBPDHDR to YI_PDHDR
  mapping for YTBPDHDR corresponding
    {
      CompanyCode        = company_code;
      CompanyName        = company_name;
      AddressLocation    = address_location;
      TaxId              = tax_id;
      LocalCreatedBy     = local_created_by;
      LocalCreatedAt     = local_created_at;
      LocalLastChangedBy = local_last_changed_by;
      LocalLastChangedAt = local_last_changed_at;
      LastChangedAt      = last_changed_at;
    }
}

define behavior for YI_PDITM alias Item
persistent table ytbpditm
lock dependent by _Header
//authorization dependent by _Header
//etag master <field_name>
{
  update;
  delete;
  field ( readonly ) CompanyCode;
  field ( mandatory ) ServiceName, BeginDate, RemainDays, EndDate, ServiceStatus;
  field ( mandatory : create, readonly : update ) ItemNumber;
  field ( readonly ) LocalCreatedBy, LocalCreatedAt, LocalLastChangedAt, LocalLastChangedBy, LastChangedAt;
  association _Header;

  //mapping all field from YTBPDITM to YI_PDITM
  mapping for YTBPDITM corresponding
    {
      CompanyCode        = company_code;
      ItemNumber         = ebelp;
      ServiceName        = service_name;
      BeginDate          = begin_date;
      EndDate            = end_date;
      RemainDays         = remain_days;
      ServiceStatus      = service_status;
      LocalCreatedBy     = local_created_by;
      LocalCreatedAt     = local_created_at;
      LocalLastChangedBy = local_last_changed_by;
      LocalLastChangedAt = local_last_changed_at;
      LastChangedAt      = last_changed_at;
    }
}
